# 07.1 Registry Deploy Action

## Build

- Add a registry package action: "Create deployment".
- Prefill package version, source, and entrypoint discovery.
- Route to deployment configuration.

## Tests

- Signed-in user can create a deployment from a workspace package.
- Public package requires install or copy into workspace before private
  deployment.
- Browser smoke covers the deploy action landmark.

## Commit

Commit as `feat: add registry deployment action`.

## Signpost

Record UX flow and smoke selectors.

