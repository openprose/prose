# Historical RFC 013 Provider Protocol Phase

This directory is kept only as a historical guardrail for stale provider
protocol planning notes.

## Current Reading

Current architecture uses the Pi graph VM and runtime profile vocabulary
documented in `docs/README.md`, `docs/inference-examples.md`, and
`docs/hosted-contract.md`.
