{
  "name": "@openprose/reactor-cli",
  "version": "0.1.0",
  "type": "commonjs",
  "description": "Deterministic command-line driver for the @openprose/reactor SDK.",
  "main": "dist/cli.js",
  "bin": {
    "reactor": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:runtime": "pnpm build && node --test $(find dist -name '*.test.js' -type f | sort)",
    "test": "pnpm typecheck && pnpm test:runtime",
    "test:offline": "REACTOR_OFFLINE=1 pnpm test"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "peerDependencies": {
    "@openprose/reactor": "workspace:*",
    "@openai/agents": "*",
    "zod": "*"
  },
  "peerDependenciesMeta": {
    "@openai/agents": {
      "optional": true
    },
    "zod": {
      "optional": true
    }
  },
  "devDependencies": {
    "@openprose/reactor": "workspace:*",
    "typescript": "^5.6.3"
  }
}
