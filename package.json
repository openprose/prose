{
  "name": "openprose-prose",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm --filter @openprose/reactor build && pnpm --filter @openprose/prose-cli build",
    "test": "pnpm --filter @openprose/reactor test && pnpm --filter @openprose/prose-cli test && pnpm test:skill",
    "test:skill": "vitest run tests/open-prose",
    "lint": "pnpm --filter @openprose/reactor run lint --if-present && pnpm --filter @openprose/prose-cli run lint --if-present"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
