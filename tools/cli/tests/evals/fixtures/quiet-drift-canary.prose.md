# Quiet Drift Canary Responsibility

When a receipt state changes without a matching public update, report `drift detected`.

This fixture is a tiny Markdown responsibility for eval plumbing. It is debug-only and not report-grade evidence.
