# Memoized Verdict Canary Responsibility

When evidence is unchanged and the prior verdict still applies, report `reused verdict`.

This fixture is a tiny Markdown responsibility for eval plumbing. It is debug-only and not report-grade evidence.
