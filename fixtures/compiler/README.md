# Compiler Fixtures

These fixtures are the first golden inputs for the RFC 006 compiler.

Tests compile them into Prose IR and assert stable structural properties beside
checked-in expected JSON fixtures.
