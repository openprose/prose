# Compiler Fixtures

These fixtures are the first golden inputs for the RFC 006 compiler.

Tests compile them into Prose IR and assert stable structural properties rather
than relying on hand-written IR snapshots while the schema is still settling.
Once the IR shape stabilizes further, add checked-in expected JSON beside these
source fixtures.

